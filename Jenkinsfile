pipeline {
    agent any
    stages {
        stage('myStage'){
            steps {
                sh 'ls -la' 
            }
        }
        stage('myStage2'){
            steps {
                sh 'ls -la'
            }
        }
        stage('myStage3'){
            steps {
                sh 'ls -la'
            }
        }
        stage('Build') {
            steps { 
                sh 'ls' 
            }
        }
    }
}
